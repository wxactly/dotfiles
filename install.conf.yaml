- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.tumx.conf: tmux.conf
    ~/.vimrc: vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
